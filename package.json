{
  "name": "kaun",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-strip-types 'tests/**/*.test.mjs'",
    "test:watch": "node --test --watch --experimental-strip-types 'tests/**/*.test.mjs'"
  },
  "devDependencies": {
    "pdfjs-dist": "^4.10.38",
    "shapefile": "^0.6.6"
  }
}
