import { ObjectPageSkeleton } from "@/components/india/ObjectPageSkeleton"

/** Money, schedule, over time — three sections on a project page. */
export default function Loading() {
  return <ObjectPageSkeleton sections={3} />
}
