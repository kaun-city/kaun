# Copy to .env.local — never commit .env.local

# URL of the Kaun FastAPI backend
# For local dev, run: docker compose up && uvicorn apps.api.main:app --reload
NEXT_PUBLIC_API_URL=http://localhost:8000
